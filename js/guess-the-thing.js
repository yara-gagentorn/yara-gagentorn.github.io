let guess = document.getElementById('my-guess').value
// take a guess and compare it with the right answer
// if partly right - reply with ""
function checkTheGuess(guess) {
  let rightAnswer = 'brown paper packages tied up with strings'
  let reply = ''
  if (guess == rightAnswer) {
    reply = 'You are right!'
  }
  else if (){

  }
}
